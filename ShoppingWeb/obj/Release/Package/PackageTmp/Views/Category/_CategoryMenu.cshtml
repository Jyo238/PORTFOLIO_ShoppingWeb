@model List<ShoppingWeb.Models.Category>


@*<div id="sidebar-wrapper">
<ul class="sidebar-nav">
    <li class="sidebar-brand">
        <a href="#">
            Menu
        </a>
    </li>
   @foreach (var item in this.Model)
   {
    <li>
        @Html.ActionLink(item.Name,
                    "Browse", "Product",
                     new { Category = item.Id }, null)

        <span style="font-size:16px; color:blue"></span>
    </li>
   }
</ul>
        </div>*@

<li class="dropdown">
    <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">開始血拚<span class="caret"></span></a>
    <ul class="dropdown-menu" role="menu">
@foreach (var item in Model)
{
    <li>@Html.ActionLink(item.Name,"Browse", "Product",new { Category = item.Id }, null)</li>
}
</ul>
</li> 
